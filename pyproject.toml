[project]
name = "pfexp"
version = "0.1.0"
description = "Particle-filter sampling experiments built on upstream open-source filters"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
cache_dir = ".build/pytest_cache"
